event: message
data: {"result":{"content":[{"type":"text","text":"### Override model setting for a single session using --settings\n\nSource: https://code.claude.com/docs/en/settings\n\nPass inline JSON to the --settings flag when launching Claude Code to apply settings for the current session without modifying persistent configuration files.\n\n```bash\nclaude --settings '{\"model\": \"claude-opus-4-8\"}'\n```\n\n--------------------------------\n\n### Configure modelSettings for specific model effort level in settings.json\n\nSource: https://code.claude.com/docs/en/settings-reference\n\nSets a per-model effort level override for Claude Code, configuring Opus 5 to use medium effort. Per-model entries take precedence over top-level effortLevel settings in the same configuration file.\n\n```json\n{\n  \"modelSettings\": {\n    \"claude-opus-5\": {\n      \"effortLevel\": \"medium\"\n    }\n  }\n}\n```\n\n--------------------------------\n\n### Configure tool permissions in ~/.claude/settings.json\n\nSource: https://code.claude.com/docs/en/settings\n\nAdd allow and deny rules in strict JSON to automatically permit lint/test commands and prevent reading .env files. Run /status inside Claude Code after saving to verify the file loaded.\n\n```json\n{\n  \"$schema\": \"https://json.schemastore.org/claude-code-settings.json\",\n  \"permissions\": {\n    \"allow\": [\n      \"Bash(npm run lint)\",\n      \"Bash(npm run test *)\"\n    ],\n    \"deny\": [\n      \"Read(./.env)\",\n      \"Read(./.env.*)\"\n    ]\n  }\n}\n```\n\n--------------------------------\n\n### Run Claude Code non-interactively with allowed tools\n\nSource: https://code.claude.com/docs/en/headless\n\nRuns Claude Code in non-interactive mode with a prompt and a list of auto-approved tools.\n\n```bash\nclaude -p \"Find and fix the bug in auth.py\" --allowedTools \"Read,Edit,Bash\"\n```\n\n### Automate and scale > Run non-interactive mode\n\nSource: https://code.claude.com/docs/en/best-practices\n\n<Tip>\n  Use `claude -p \"prompt\"` in CI, pre-commit hooks, or scripts. Add `--output-format stream-json --verbose` for streaming JSON output.\n</Tip>\n\nWith `claude -p \"your prompt\"`, you can run Claude non-interactively, without an interactive prompt. The run still creates a resumable session unless you pass `--no-session-persistence`. [Non-interactive mode](/docs/en/headless) is how you integrate Claude into CI pipelines, pre-commit hooks, or any automated workflow. The output formats let you parse results programmatically: plain text, JSON, or streaming JSON.\n\nThe first command prints plain text. The `json` format returns a single JSON object with a `result` field. The `stream-json` format prints one JSON object per line, starting with an init event."}]},"jsonrpc":"2.0","id":3}

