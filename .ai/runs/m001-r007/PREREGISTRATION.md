# R007 preregistration — reserved simulated validation

R007 is frozen before opening. It is a **SIMULATED reserved holdout**, not a physical validation. No R007 frame has been generated, no reconstruction has run, truth has not been imported for R007, and no R007 score or result exists at this boundary.

## Candidate and estimator

The only candidate is the D004 five-view geometry: level, zero-orientation captures at `(0.1225, 0.1225, 0.40)`, `(0.1775, 0.1225, 0.40)`, `(0.1225, 0.1775, 0.40)`, `(0.1775, 0.1775, 0.40)`, and `(0.15, 0.15, 0.40)` metres. R007 uses fresh sequences 701–705 for all five views. The estimator is the unchanged D003 pooled-XYZ Delaunay implementation.

No pose, sequence assignment, estimator parameter, eligibility rule, scene, or gate may change after opening. Development frames are forbidden. Every input frame must be newly generated by the frozen simulator binary.

## Fixed scene table

The following 12 nonempty scenes are the complete holdout. None is identical to a D003/D004 scene, all of which used fill 75. No scene may be selected, removed, replaced, rerun under a new sequence, or treated as exploratory after opening.

| # | scene | fill | shape | cx | beam | dropout |
|---:|---|---:|---:|---:|---:|---:|
| 1 | `r007_pyramid_low_left` | 25 | 0 | 0.11 | 0 | 0 |
| 2 | `r007_pyramid_high_right` | 95 | 0 | 0.19 | 0 | 0 |
| 3 | `r007_pyramid_mid_right_beam` | 55 | 0 | 0.17 | 1 | 0 |
| 4 | `r007_pyramid_high_left_dropout35` | 85 | 0 | 0.12 | 0 | 35 |
| 5 | `r007_two_stacks_low` | 30 | 1 | 0.14 | 0 | 0 |
| 6 | `r007_two_stacks_high_beam` | 90 | 1 | 0.16 | 1 | 0 |
| 7 | `r007_prism_low_left` | 35 | 2 | 0.12 | 0 | 0 |
| 8 | `r007_prism_high_right_beam_dropout10` | 90 | 2 | 0.18 | 1 | 10 |
| 9 | `r007_ramp_low_left_beam` | 25 | 3 | 0.11 | 1 | 0 |
| 10 | `r007_ramp_high_right_dropout25` | 95 | 3 | 0.19 | 0 | 25 |
| 11 | `r007_layer_low_dropout10` | 20 | 4 | 0.14 | 0 | 10 |
| 12 | `r007_layer_high_beam_dropout30` | 90 | 4 | 0.16 | 1 | 30 |

Shapes 0–4 are all present. The matrix includes low and high fill, shifted pyramids, the supported beam obstacle, deterministic dropout, and combined obstacle/dropout stress. Fill remains above zero so the preregistered relative-error gate is defined for every scene.

## Acquisition and truth boundary

Acquisition is exactly 12 scenes × 5 new frames = 60 unique inputs. The acquisition runner has no estimator, geometry, or truth import. It verifies the pre-open hashes and an independent release bound to the pre-open manifest before it invokes the simulator.

After acquisition, a truth-free validation must confirm the exact 60-file matrix, frame format, CRC, sequence, pose, simulator hash, command log, absence of reused frames, and absence of results. A second independent release bound to the acquisition manifest and validation is required before scoring.

The scoring runner reconstructs all 12 scenes with the frozen D003 estimator before `experiments.truth` is imported inside the evaluator function. Truth is used only to calculate metrics after reconstruction.

## Primary gate

R007 passes only if all 12 scenes produce eligible total volume over all 400 cells and every scene satisfies:

`abs(estimated_total_m3 - exact_true_total_m3) / exact_true_total_m3 <= 0.05`

Any partial, unavailable, invalid, or otherwise ineligible scene fails the full gate. An average, median, pooled score, family score, or success subset cannot substitute for 12/12. Unsupported cells are never filled with zero.

Signed total error, integrated absolute cell-volume error, spatial error, cancellation ratio, support, unsupported truth volume, hull diagnostics, rejection counts, point counts, merge conflicts, triangle counts, and local runtime are mandatory reports. Spatial error and cancellation do not alter or rescue the primary gate.

## Frozen chain and interpretation

`pre-open-manifest.json` records SHA-256 for the protocol and its detached checksum, estimator, acquisition runner, scoring/evaluator runner, tests, simulator source and binary, truth evaluator, runtime geometry, volume requirement, D004 evidence supporting promotion, this preregistration, the release request, and the README. The independent reviewer must recalculate these hashes and bind the manifest hash in the release.

The result, whether pass or fail, remains simulation evidence. It does not demonstrate accuracy on hardware, robustness to real optical effects, or deployment readiness.
